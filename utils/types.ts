export interface flightType {
    id:number,
    image: string,
    departure: string,
    deprt_initial: string,
    arrival: string,
    arrival_initial: string,
    flight_price: string
}

export interface faqType {
    id: number,
    question: string,
    answers: string
}